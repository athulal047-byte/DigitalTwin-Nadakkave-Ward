# Application
NODE_ENV=production
PORT=3000

# Database (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASS=your_db_password
DB_NAME=nadakkave

# JWT Secrets
JWT_SECRET=generate_a_secure_random_string_here
JWT_EXPIRES_IN=24h

# Redis (for PubSub / Rate Limiting)
REDIS_URL=redis://localhost:6379

# Logging
LOG_LEVEL=info
